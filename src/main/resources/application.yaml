spring:
  application:
    name: rate-limiter
  data:
    redis:
      host: localhost
      port: 6379
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
rate-limiter:
  free:
    capacity: 4
    per-minute: 4
  premium:
    capacity: 100
    per-minute: 100